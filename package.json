{
  "name": "trello-hk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "mvn -f apps/backend spring-boot:run",
    "dev:websocket": "mvn -f apps/websocket spring-boot:run",
    "dev:frontend": "npm --prefix apps/frontend run dev",
    "dev": "concurrently -n \"backend,websocket,frontend\" -c \"blue,magenta,cyan\" \"npm run dev:backend\" \"npm run dev:websocket\" \"npm run dev:frontend\"",
    "build:java": "mvn clean install",
    "build:frontend": "npm --prefix apps/frontend run build",
    "build": "npm run build:java && npm run build:frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
